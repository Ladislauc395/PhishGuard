name: phishguard_app
description: PhishGuard - Your Digital Security Shield
publish_to: 'none'
version: 1.0.0+1
dev_dependencies:
  flutter_test:
    sdk: flutter

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  fl_chart: ^0.66.0
  percent_indicator: ^4.2.3
  google_fonts: ^6.1.0
  intl: ^0.19.0
  url_launcher: ^6.2.5
  google_sign_in: ^6.2.1          # Google OAuth
  flutter_secure_storage: ^9.0.0  # Armazenar token JWT encriptado
  http: ^1.2.1                    # Chamadas HTTP (se não tiver já)
  jose: ^0.3.5 
  shared_preferences: ^2.2.3   
  web_socket_channel: ^2.4.0            
  firebase_core: ^2.24.0
  firebase_messaging: ^14.7.0
  flutter_local_notifications: ^18.0.0

flutter:
  uses-material-design: true
